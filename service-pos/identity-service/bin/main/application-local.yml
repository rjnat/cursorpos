spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cursorpos
    username: posuser
    password: ${DB_PASSWORD:pos_db_password_2025}
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379
      password: ${REDIS_PASSWORD:redis_dev_password_2025}

jwt:
  secret: ${JWT_SECRET:cursorpos-jwt-secret-key-2025-minimum-256-bits-required-for-hs256-algorithm-security}
